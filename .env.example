# Copy this file to .env and fill in your own values.
# The project reads plain environment variables, it does not load .env on its
# own. Export them before starting the server:
#
#   set -a; source .env; set +a

DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=False
